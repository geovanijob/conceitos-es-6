//Object Short Syntax

const nome = 'Geovani';
const idade = 25;

const usuario = {
    // nome: nome, // quando o nome no objeto é o mesmo nome da variavel pode deixar só um nome
    nome,
    idade,
    empresa: 'CodeJob',
};

console.log(usuario);